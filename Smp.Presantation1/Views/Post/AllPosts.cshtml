@model List<GetPostVM>
@{
    ViewData["Title"] = "AllPosts";
}


<form id="allPosts" novalidate class="needs-validation">

    <div class="col-sm-6">
        <div class="row">
            <div class="card-body">
                @foreach (var item in Model)
                {
                    <form asp-action="UserProfile" asp-route-returnUrl="@ViewData["ReturnUrl"]">
                        <div class="card card-widget">
                            <div class="card-header">
                                <div class="user-block">
                                    <img class="img-circle" src="@item.UserImagePath" alt="User Image">
                                 	 <a asp-controller="User" asp-action="Details" asp-route-id="@item.User_Id">@item.UserName</a>
                                    <span class="description">Shared publicly - @item.CreateDate</span>
                                </div>
                                <!-- /.user-block -->
                        </div>
                            <!-- /.card-header -->
                        <div class="card-body">
                                <img class="img-fluid pad" src="@item.ImagePath" alt="Photo">
                                <br />
                                <h4 class="card-text">@item.Description</h4>
                                <a asp-controller="Post" asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Details</a>
                                <a asp-controller="FavoritePost" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Delete</a>
                                <span class="float-right text-muted">@item.Total_Score Score - @item.Total_Comment Comments</span>
                            </div>
                            <!-- /.card-body -->


                    </div>
                    </form>
                }
            </div>
            </div>
            <div class="card-footer">
                <a asp-action="Create" class="btn btn-primary">Create New One</a>
            </div>
        </div>
    </div>



</form>

